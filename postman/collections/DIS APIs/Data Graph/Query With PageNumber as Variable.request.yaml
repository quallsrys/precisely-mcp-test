$kind: http-request
url: https://{{host}}/data-graph/graphql
method: POST
headers:
  cache-disabled: "true"
body:
  type: graphql
  content:
    query: ""
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 1000
