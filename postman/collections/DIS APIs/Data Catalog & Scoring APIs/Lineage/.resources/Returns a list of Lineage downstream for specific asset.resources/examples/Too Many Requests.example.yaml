$kind: http-example
request:
  url: "{{baseUrl}}/v2/catalog/lineage/:assetId/downstream"
  method: GET
  headers:
    - key: Accept
      value: application/json
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    assetId: ""
response:
  statusCode: 429
  statusText: Too Many Requests
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "statusCode": 429,
        "error": "Too Many Requests",
        "message": "Rate limit exceeded"
      }
order: 5000
