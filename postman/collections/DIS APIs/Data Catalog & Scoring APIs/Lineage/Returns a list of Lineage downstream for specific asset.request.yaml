$kind: http-request
description: Returns a list of Lineage downstream for specific asset
url: https://{{host}}/v2/catalog/lineage/:assetId/downstream
method: GET
headers:
  Accept: application/json
pathVariables:
  assetId: 6Db28DFFbDbf177Eab23bDeC
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
examples: ./.resources/Returns a list of Lineage downstream for specific asset.resources/examples
order: 3000
