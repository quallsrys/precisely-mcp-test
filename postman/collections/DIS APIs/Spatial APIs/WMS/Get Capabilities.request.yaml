$kind: http-request
url: https://{{host}}/v1/spatial/wms?VERSION=1.3.0&SERVICE=WMS&REQUEST=GetCapabilities
method: GET
queryParams:
  VERSION: 1.3.0
  SERVICE: WMS
  REQUEST: GetCapabilities
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 3000
